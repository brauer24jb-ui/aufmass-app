workflows:
  default-workflow:
    name: Aufmass App TestFlight Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      groups:
        - appstore_credentials
      flutter: stable
      
      # DIE MAGIE: Dieser Block sagt Codemagic, dass es deinen neuen UI-Tresor nutzen soll!
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.nordwest.aufmassapp
        
    scripts:
      - name: Restore Xcode Automatic Signing
        script: |
          # Löscht unsere alten manuellen "Hacks" aus der Datei, da wir sie nicht mehr brauchen
          sed -i '' 's/CODE_SIGN_STYLE = Manual;/CODE_SIGN_STYLE = Automatic;/g' ios/Runner.xcodeproj/project.pbxproj
          sed -i '' 's/ProvisioningStyle = Manual;/ProvisioningStyle = Automatic;/g' ios/Runner.xcodeproj/project.pbxproj
          
      - name: Flutter build iOS
        script: |
          flutter clean
          flutter pub get
          
          # Baut die App und nutzt die von Codemagic generierte Export-Datei
          flutter build ipa --release --export-options-plist=$HOME/export_options.plist
          
      - name: Upload to TestFlight
        script: |
          app-store-connect publish --ipa "build/ios/ipa/*.ipa"
    artifacts:
      - build/ios/ipa/*.ipa