workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      groups:
        - appstore_credentials
      flutter: stable
      vars:
        # HIER IST DIE MAGIE: Diese Variablen zwingen Xcode im Hintergrund zum Distribution-Zertifikat!
        FLUTTER_XCODE_CODE_SIGN_STYLE: "Manual"
        FLUTTER_XCODE_CODE_SIGN_IDENTITY: "Apple Distribution"
    scripts:
      - name: iOS Code Signing Setup
        script: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$APP_STORE_CONNECT_PRIVATE_KEY" > ~/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_KEY_IDENTIFIER.p8
          
          keychain initialize
          
          # Holt ausschließlich das App Store Profil
          app-store-connect fetch-signing-files "com.nordwest.aufmassapp" \
            --type IOS_APP_STORE \
            --issuer-id "$APP_STORE_CONNECT_ISSUER_ID" \
            --key-id "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --private-key ~/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_KEY_IDENTIFIER.p8 \
            --create
            
          keychain add-certificates
          
          # Trägt die Profile ein und erstellt die Plist-Datei
          xcode-project use-profiles
          
      - name: Flutter build iOS
        script: |
          flutter pub get
          
          # Ein ganz sauberer, normaler Flutter-Befehl ohne fehlerhafte Anhänge!
          flutter build ipa --release --export-options-plist=$HOME/export_options.plist
          
      - name: Upload to TestFlight
        script: |
          app-store-connect publish \
            --issuer-id "$APP_STORE_CONNECT_ISSUER_ID" \
            --key-id "$APP_STORE_CONNECT_KEY_IDENTIFIER" \
            --private-key ~/.appstoreconnect/private_keys/AuthKey_$APP_STORE_CONNECT_KEY_IDENTIFIER.p8 \
            --ipa "build/ios/ipa/*.ipa"
    artifacts:
      - build/ios/ipa/*.ipa